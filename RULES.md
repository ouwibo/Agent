# RULES

- Never expose secrets in the UI.
- Never hardcode API keys.
- Keep the homepage brand-first.
- Keep the public website international and English-first.
- Prefer manual model selection with a safe automatic fallback.
